<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/VisualizerView (snapshot).cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/VisualizerView (snapshot).cs" />
              <option name="originalContent" value="using DevToys.Api;&#10;using KnifeSQLExtension.Features.RandomDataGeneration.Services;&#10;using KnifeSQLExtension.Features.SqlVisualizer.Services;&#10;using Microsoft.IdentityModel.Tokens;&#10;using static DevToys.Api.GUI;&#10;using Microsoft.Extensions.Logging;&#10;&#10;namespace KnifeSQLExtension.UI.Views&#10;{&#10;    public class VisualizerView : IView&#10;    {&#10;        // Shared session &#10;        private readonly SqlSession _session;&#10;        private readonly ILogger&lt;VisualizerView&gt; _logger;&#10;        private readonly ILoggerFactory _loggerFactory;&#10;&#10;        // Services&#10;        private VisualizerService _service;&#10;        private TableService _tableService;&#10;        &#10;        private readonly IUIWebView _webView = WebView();&#10;&#10;        // Table action buttons&#10;        private readonly IUIButton _refreshButton = Button().Text(&quot;Refresh&quot;).AccentAppearance();&#10;&#10;        public VisualizerView(SqlSession session, ILogger&lt;VisualizerView&gt; logger, ILoggerFactory loggerFactory)&#10;        {&#10;            _session = session;&#10;            _logger = logger;&#10;            _loggerFactory = loggerFactory;&#10;        }&#10;&#10;  &#10;        public IUIElement View =&gt; BuildUI();&#10;&#10;   &#10;        public async Task Init()&#10;        {&#10;            _logger.LogInformation(&quot;Initializing VisualizerView&quot;);&#10;            _tableService = new TableService(_session.GetDbClient(), _loggerFactory.CreateLogger&lt;TableService&gt;());&#10;            _service = new VisualizerService(_tableService);&#10;            &#10;            // var nodes = await _service.CreateNodes();&#10;            // var html = _service.GenerateSvgNodes(nodes);&#10;            &#10;            // Get the directory where the assembly is loaded&#10;            string assemblyDirectory = Path.GetDirectoryName(typeof(VisualizerView).Assembly.Location)!;&#10;            &#10;            // Construct the path to the React dist folder (cross-platform)&#10;            string distPath = Path.Combine(assemblyDirectory, &quot;dist&quot;);&#10;            string htmlPath = Path.Combine(distPath, &quot;index.html&quot;);&#10;&#10;            if (File.Exists(htmlPath))&#10;            {&#10;                try&#10;                {&#10;                    // Read the HTML file&#10;                    string htmlContent = await File.ReadAllTextAsync(htmlPath);&#10;                    &#10;                    // Replace relative asset paths with absolute file:// URIs&#10;                    // This allows the webview to load CSS, JS, and image assets&#10;                    Uri distUri = new Uri(distPath, UriKind.Absolute);&#10;                    string distUriString = distUri.AbsoluteUri.TrimEnd('/');&#10;                    &#10;                    // Replace relative paths with absolute file:// URIs&#10;                    htmlContent = htmlContent.Replace(&quot;./&quot;, distUriString + &quot;/&quot;);&#10;                    &#10;                    _logger.LogInformation(&quot;Loading React app from: {Path}&quot;, htmlPath);&#10;                    _webView.RenderHTML(htmlContent);&#10;                }&#10;                catch (Exception ex)&#10;                {&#10;                    _logger.LogError(ex, &quot;Failed to load React app HTML&quot;);&#10;                }&#10;            }&#10;            else&#10;            {&#10;                _logger.LogError(&quot;Could not find index.html at {Path}&quot;, htmlPath);&#10;            }&#10;            &#10;            // await RefreshTables();&#10;            _logger.LogInformation(&quot;VisualizerView initialized successfully&quot;);&#10;        }&#10;        &#10;        &#10;        &#10;        private IUIElement BuildUI()&#10;        {&#10;&#10;            return Stack()&#10;                .LargeSpacing()&#10;                .Vertical()&#10;                .WithChildren(&#10;                    _webView&#10;                );&#10;        }&#10;&#10;        private void OnRefreshClicked()&#10;        {&#10;            if (!_session.IsConnected || _session.GetDbClient() is null)&#10;            {&#10;                return;&#10;            }&#10;&#10;            Task.Run(async () =&gt;&#10;            {&#10;                await RefreshTables(true);&#10;            });&#10;        }&#10;        &#10;        private async Task RefreshTables(bool forceRefresh = false)&#10;        {&#10;            if (!_session.IsConnected || _session.GetDbClient() is null)&#10;            {&#10;                return;&#10;            }&#10;            &#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="using DevToys.Api;&#10;using KnifeSQLExtension.Features.RandomDataGeneration.Services;&#10;using KnifeSQLExtension.Features.SqlVisualizer.Services;&#10;using Microsoft.IdentityModel.Tokens;&#10;using static DevToys.Api.GUI;&#10;using Microsoft.Extensions.Logging;&#10;&#10;namespace KnifeSQLExtension.UI.Views&#10;{&#10;    public class VisualizerView : IView&#10;    {&#10;        // Shared session &#10;        private readonly SqlSession _session;&#10;        private readonly ILogger&lt;VisualizerView&gt; _logger;&#10;        private readonly ILoggerFactory _loggerFactory;&#10;&#10;        // Services&#10;        private VisualizerService _service;&#10;        private TableService _tableService;&#10;        &#10;        private readonly IUIWebView _webView = WebView();&#10;&#10;        // Table action buttons&#10;        private readonly IUIButton _refreshButton = Button().Text(&quot;Refresh&quot;).AccentAppearance();&#10;&#10;        public VisualizerView(SqlSession session, ILogger&lt;VisualizerView&gt; logger, ILoggerFactory loggerFactory)&#10;        {&#10;            _session = session;&#10;            _logger = logger;&#10;            _loggerFactory = loggerFactory;&#10;        }&#10;&#10;  &#10;        public IUIElement View =&gt; BuildUI();&#10;&#10;   &#10;        public async Task Init()&#10;        {&#10;            _logger.LogInformation(&quot;Initializing VisualizerView&quot;);&#10;            _tableService = new TableService(_session.GetDbClient(), _loggerFactory.CreateLogger&lt;TableService&gt;());&#10;            _service = new VisualizerService(_tableService);&#10;            &#10;            // var nodes = await _service.CreateNodes();&#10;            // var html = _service.GenerateSvgNodes(nodes);&#10;            &#10;            // Get the directory where the assembly is loaded&#10;            string assemblyDirectory = Path.GetDirectoryName(typeof(VisualizerView).Assembly.Location)!;&#10;            &#10;            // Construct the path to the React dist folder (cross-platform)&#10;            string distPath = Path.Combine(assemblyDirectory, &quot;dist&quot;);&#10;            string htmlPath = Path.Combine(distPath, &quot;index.html&quot;);&#10;&#10;            if (File.Exists(htmlPath))&#10;            {&#10;                try&#10;                {&#10;                    // Read the HTML file&#10;                    string htmlContent = await File.ReadAllTextAsync(htmlPath);&#10;                    &#10;                    // Create absolute file:// URI for the dist directory&#10;                    Uri distUri = new Uri(distPath + &quot;/&quot;, UriKind.Absolute);&#10;                    string baseHref = distUri.AbsoluteUri;&#10;                    &#10;                    // Insert &lt;base&gt; tag in &lt;head&gt; to make relative paths work&#10;                    // This tells the browser to resolve all relative URLs from the dist directory&#10;                    string baseTag = $&quot;&lt;base href=\&quot;{baseHref}\&quot; /&gt;&quot;;&#10;                    htmlContent = htmlContent.Replace(&quot;&lt;/head&gt;&quot;, $&quot;{baseTag}&lt;/head&gt;&quot;);&#10;                    &#10;                    _logger.LogInformation(&quot;Loading React app from: {Path}&quot;, htmlPath);&#10;                    _logger.LogInformation(&quot;Base URL set to: {BaseUrl}&quot;, baseHref);&#10;                    _webView.RenderHTML(htmlContent);&#10;                }&#10;                catch (Exception ex)&#10;                {&#10;                    _logger.LogError(ex, &quot;Failed to load React app HTML&quot;);&#10;                }&#10;            }&#10;            else&#10;            {&#10;                _logger.LogError(&quot;Could not find index.html at {Path}&quot;, htmlPath);&#10;            }&#10;            &#10;            // await RefreshTables();&#10;            _logger.LogInformation(&quot;VisualizerView initialized successfully&quot;);&#10;        }&#10;        &#10;        &#10;        &#10;        private IUIElement BuildUI()&#10;        {&#10;&#10;            return Stack()&#10;                .LargeSpacing()&#10;                .Vertical()&#10;                .WithChildren(&#10;                    _webView&#10;                );&#10;        }&#10;&#10;        private void OnRefreshClicked()&#10;        {&#10;            if (!_session.IsConnected || _session.GetDbClient() is null)&#10;            {&#10;                return;&#10;            }&#10;&#10;            Task.Run(async () =&gt;&#10;            {&#10;                await RefreshTables(true);&#10;            });&#10;        }&#10;        &#10;        private async Task RefreshTables(bool forceRefresh = false)&#10;        {&#10;            if (!_session.IsConnected || _session.GetDbClient() is null)&#10;            {&#10;                return;&#10;            }&#10;            &#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>